<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.suseok.run.model.dao.RecordDao">

    <insert id="insertRecord">
        INSERT INTO records (distance, run_time, pace, user_seq)
        VALUES (#{distance}, #{runTime}, #{pace}, #{userSeq})
    </insert>
    
    <select id="selectRecordsByUserId" parameterType="String">
    	select * from records
    	where user_seq = (SELECT user_seq FROM User WHERE user_id = #{userId})
    </select>
    
       <select id="selectRecordsByUserSeq" parameterType="int">
    	select * from records
    	where user_seq = #{userSeq}
    </select>

</mapper>
