<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suseok.run.model.dao.UserDao">


  <!-- 모든 사용자 목록을 조회하는 SQL -->
  <select id="selectAll" resultType="User">
    SELECT *
    FROM user
  </select>

  <!-- 사용자를 추가하는 SQL -->
  <insert id="insert" parameterType="User">
    INSERT INTO user (user_id, user_pwd, user_name, user_nick, email, address, img, phone, exposure)
    VALUES (#{userId}, #{userPwd}, #{userName}, #{userNick}, #{email}, #{address}, #{img}, #{phone}, #{exposure})
  </insert>

  <!-- 사용자 정보를 업데이트하는 SQL -->
  <update id="update" parameterType="User">
    UPDATE user
    SET user_pwd = #{userPwd}, user_nick = #{userNick}, 
        email = #{email}, address = #{address}, img = #{img}, phone = #{phone}, exposure = #{exposure}
    WHERE user_seq = #{userSeq}
  </update>

  <!-- 사용자를 삭제하는 SQL -->
  <delete id="delete" parameterType="int">
    DELETE FROM user
    WHERE user_seq = #{userSeq}
  </delete>


	

</mapper>
