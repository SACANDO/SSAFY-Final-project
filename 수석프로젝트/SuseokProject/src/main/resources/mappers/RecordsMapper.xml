<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suseok.run.basic.model.dao.UserDao">


    <!-- 기록 등록 -->
    <insert id="insert" parameterType="Records">
        INSERT INTO records (date, distance, run_time, pace, user_seq)
        VALUES (#{date}, #{distance}, #{runTime}, #{pace}, #{userSeq})
    </insert>

    <!-- 전체 기록 선택 -->
    <select id="selectAll" resultType="Records">
        SELECT id, date, distance, run_time, pace, user_seq 
        FROM records
    </select>

    <!-- 기록 갱신 -->
    <update id="update" parameterType="Records">
        UPDATE records
        SET date = #{date}, distance = #{distance}, run_time = #{runTime}, pace = #{pace}, user_seq = #{userSeq}
        WHERE id = #{id}
    </update>

    <!-- 기록 삭제 -->
    <delete id="delete" parameterType="int">
        DELETE FROM records 
        WHERE id = #{id}
    </delete>

</mapper>
